<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registration form</title>
  <link rel="stylesheet" href="form-style.css">
</head>

<body>

  <div class="container">
    <form class="form" id="form">

      <p> Registration</p>

      <form action="/signup" method="POST"></form>

      <div class="form-input">
        <input type="text" name="username" placeholder="User name" id="myname">
        <label for="myname">User</label>
        {{#if errorMessage}}
        <p class="error">{{errorMessage}}</p>
        {{/if}}
      </div>

      <div class="form-input">
        <input type="password" name="password" placeholder="Enter password" id="password">
        <label for="password">Password</label>
        {{#if errorMessage}}
        <p class="error">{{errorMessage}}</p>
        {{/if}}
      </div>
      <div class="form-input">
        <input type="password" name="verifyPassword" placeholder="Repeat password" id="verifyPassword">
        <label for="verifyPassword">Verify password</label>
        {{#if errorMessage}}
        <p class="error">{{errorMessage}}</p>
        {{/if}}
      </div>

      <div class="form-input">
        <input type="email" name="email" placeholder="Enter email" id="email">
        <label for="email">E-mail</label>
        {{#if errorMessage}}
        <p class="error">{{errorMessage}}</p>
        {{/if}}
      </div>


      <div class="button-SignUp">
        <button type="submit">Sign up</button>
      </div>

    </form>
  </div>


</body>

</html>